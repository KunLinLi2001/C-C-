打开.dev文件 点击运行会进入主页面，有着相关的操作。如果想要读取文件内容是选择读取存档那个指令，这样会把本来带有的火车信息、购票信息两个文件的内容导入链表。
老师我在代码里面加了足够多的注释，因此每一步都会有中文的指令，可以顺利完成所有的操作
此外在课下修改了根据票价查询车次的bug
ps：不确定换一台电脑以后读取存档是否会有问题，因为上次我用自己的电脑读取了记事本中的内容，能成功读入链表；但是用机房的就会出问题。如果导入信息依旧有问题，不是因为代码bug，可能是兼容性的问题。